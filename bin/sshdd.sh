#! /bin/bash
# reference: http://www.linuxweblog.com/node/282

echo "below is just an example,please change port, user, ip and remote_ssh_port"
echo "ssh -N -f -C -g -R port:localhost:22 user@ip -p remote_ssh_port"
